set invisible char in entry
add const for args of functions
in gml_bison.y change name of common rules
add bison, flex and gtk3-devel check in configure script
rebuild GtkTextBuffer
del duplicate Labe and Label_text in gml_flex.l
fix the sequence of icons on mic_button
add support action groups
add margin all sides into CMN  
add MyText widget

