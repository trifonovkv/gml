make example with scrolled window and text view
change example directory
corect README file
check build separate directory
add in generated c file EOF
check on dublicate title in header bar and window
add application window
add stack add titled
change add for header bar (hb_pack) ?
add other last stack add
stack_add move in add
